# Configuración de Email/SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@livex.com

# Para desarrollo con Mailhog
# SMTP_HOST=localhost
# SMTP_PORT=1025

# Para producción (ejemplo con Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# URL del frontend para enlaces en emails
FRONTEND_URL=https://livex.com

# RabbitMQ (ya configurado en .env principal)
# AMQP_URL=amqp://livex:livex@rabbitmq:5672

# Base de datos (ya configurado en .env principal)
# DATABASE_URL=postgresql://livex:livex@db:5432/livex
